import 'package:flutter/material.dart';

class AllTopChartsPage extends StatelessWidget {
  static const id = 'all_top_charts_page';
  const AllTopChartsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('AllTopChartsPage')));
  }
}
